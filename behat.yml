default:
  suites:
    api:
      paths:
        - '%paths.base%/features/api'
      contexts:
        - App\Tests\Behat\ApiContext

  extensions:
    Behat\MinkExtension:
      base_url: 'http://localhost:8080'
      sessions:
        default:
          symfony: ~

