# Specjalny routing dla stron błędów w dev nie jest wymagany w nowszych wersjach Symfony.
# Po migracji na Symfony 8 można bezpiecznie usunąć import starego errors.xml,
# a obsługa błędów będzie realizowana przez domyślny ErrorController.

# Jeśli w przyszłości będziesz mieć własny ErrorController z atrybutami,
# możesz tu dodać odpowiednią konfigurację routingu.
